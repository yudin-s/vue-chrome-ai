# Vue Chrome AI Studio

Live GitHub Pages demo for `@yudin-s/vue-chrome-ai`.

```bash
npm install
npm run dev
npm run build
```

The app is intentionally built against the local package with `@yudin-s/vue-chrome-ai: file:../..` so CI verifies the current package build before deploying the demo.
